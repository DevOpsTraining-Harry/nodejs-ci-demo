pipeline {
  agent any
  stages {
    stage('Build & Test') {
      steps {
        echo "Building frontend and backend"
        dir('frontend') {
          sh 'docker build -t frontend-app .'
        }
        dir('backend') {
          sh 'docker build -t backend-app .'
        }
      }
    }
  }
}
